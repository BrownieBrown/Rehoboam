# Run rehoboam trading bot every 4 hours
# Format: minute hour day month weekday command
0 */4 * * * cd /app && /usr/local/bin/rehoboam trade --max 5 >> /var/log/rehoboam/trade.log 2>&1

# Run at startup (once)
@reboot cd /app && /usr/local/bin/rehoboam trade --max 5 >> /var/log/rehoboam/trade.log 2>&1

# Empty line required at end of cron file
